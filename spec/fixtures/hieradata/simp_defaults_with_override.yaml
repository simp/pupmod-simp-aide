---
compliance_engine::enforcement:
  - simp:defaults
# Explicit site override. It sits at a higher Hiera priority than the compliance
# engine level, so it must win over the profile's value for aide::dbdir.
aide::dbdir: '/opt/custom/aide'
